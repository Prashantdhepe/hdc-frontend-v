export default {
    services(state) {
        return state.services;
    },

    service(state) {
        return state.service;
    },

    datatable(state) {
        return state.datatable;
    },

    meta(state) {
        return state.pagination;
    }
}
